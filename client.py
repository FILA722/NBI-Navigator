class Client:
    def __init__(self, name, nickname, notes, ip, is_active, email, phone, switch, port, saved_mac, current_mac, port_is_active, errors_on_port, cacti_pic):
        self.name = name,
        self.nickname = nickname,
        self.notes = notes
        self.ip = ip,
        self.is_active = is_active,
        self.email = email,
        self.phone = phone,
        self.switch = switch,
        self.port = port,
        self.saved_mac = saved_mac,
        self.current_mac = current_mac,
        self.port_is_active = port_is_active,
        self.errors_on_port = errors_on_port
        self.cacti_pic = cacti_pic

